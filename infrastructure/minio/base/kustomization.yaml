---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - secret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingressroute.yaml
  - init-job.yaml

labels:
- includeSelectors: false
  includeTemplates: true
  pairs:
    app: minio

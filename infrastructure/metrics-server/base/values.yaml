# Base Helm values for metrics-server
# Only includes non-default values necessary for our infrastructure

# High availability - run 2 replicas with anti-affinity
replicas: 2

# Pod disruption budget for availability
podDisruptionBudget:
  enabled: true
  minAvailable: 1

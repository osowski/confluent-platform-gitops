---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-data
  namespace: registry
  labels:
    app: registry
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      # Sized for several CI-built images; cp-flink base layers are ~2GB+ each
      storage: 20Gi

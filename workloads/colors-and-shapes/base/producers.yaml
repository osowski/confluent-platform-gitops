---
# Shapes Producer Deployment
# Writes messages to shapes-input topic
# Default replicas: 0 (scale to 1 to enable)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shapes-producer
  namespace: flink-shapes
  labels:
    app: shapes-producer
spec:
  replicas: 0  # Disabled by default - scale to 1 to enable
  selector:
    matchLabels:
      app: shapes-producer
  template:
    metadata:
      labels:
        app: shapes-producer
    spec:
      containers:
        - name: producer
          image: quay.io/osowski/kafka-producer:v0.2.0-avro
          imagePullPolicy: Always
          env:
            # Kafka connection (internal listener)
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "kafka.kafka.svc.cluster.local:9071"

            # Topic configuration
            - name: KAFKA_TOPIC
              value: "shapes-input"

            # Data type label (for logging/identification only - all producers generate same sensor data)
            - name: DATA_TYPE
              value: "shapes"

            # Producer rate (messages per second)
            - name: MESSAGE_RATE
              value: "10"

            # Schema Registry configuration
            - name: SCHEMA_REGISTRY_URL
              value: "http://schemaregistry.kafka.svc.cluster.local:8081"

          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
---
# Colors Producer Deployment
# Writes messages to colors-input topic
# Default replicas: 0 (scale to 1 to enable)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: colors-producer
  namespace: flink-colors
  labels:
    app: colors-producer
spec:
  replicas: 0  # Disabled by default - scale to 1 to enable
  selector:
    matchLabels:
      app: colors-producer
  template:
    metadata:
      labels:
        app: colors-producer
    spec:
      containers:
        - name: producer
          image: quay.io/osowski/kafka-producer:v0.2.0-avro
          imagePullPolicy: Always
          env:
            # Kafka connection (internal listener)
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "kafka.kafka.svc.cluster.local:9071"

            # Topic configuration
            - name: KAFKA_TOPIC
              value: "colors-input"

            # Data type label (for logging/identification only - all producers generate same sensor data)
            - name: DATA_TYPE
              value: "colors"

            # Producer rate (messages per second)
            - name: MESSAGE_RATE
              value: "10"

            # Schema Registry configuration
            - name: SCHEMA_REGISTRY_URL
              value: "http://schemaregistry.kafka.svc.cluster.local:8081"

          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi

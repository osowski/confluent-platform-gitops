apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Label the kafka namespace as a participant in the mTLS trust mesh so the
# trust-manager Bundle (rbac-mtls-ca-bundle) distributes the CA ConfigMap here.
# Adding this label to flink/operator extends trust to those namespaces (#275, Epic #271).
patches:
  - patch: |-
      apiVersion: v1
      kind: Namespace
      metadata:
        name: kafka
        labels:
          mtls-trust: enabled

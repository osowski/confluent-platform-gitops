---
# CMF credential chain for the generic `default` environment's Flink SQL demo
# on RBAC clusters (no anonymous Kafka/SR listener here, unlike flink-demo).
#
# Unlike shapes/colors (which each have their own sa-<group>-flink Keycloak
# service account for Schema Registry auth), the `default` environment has no
# dedicated per-tenant identity - it's the generic, ungrouped demo. Both the
# Kafka and Schema Registry connections use the same `cmf` service account
# already used for CFK's own CMFRestClass client-credentials flow (see
# cmfrestclass-oauth-patch.yaml) and already granted Kafka superuser access
# on every RBAC cluster.
#
# NAMING IS CRITICAL - all THREE names must be identical:
#
#   FlinkSecret.metadata.name
#     == FlinkEnvironmentSecretMapping.metadata.name
#     == the connectionSecretId / connectionSecretRef that references them
#
# Two separate mechanisms enforce this, and only one of them is documented:
#
#   * CMF keys an environment's secrets by the mapping name, and silently
#     ignores a catalog whose connectionSecretId is unmapped rather than
#     returning an error.
#   * CFK resolves connectionSecretId to a FlinkSecret resource in the same
#     namespace. A mismatch fails reconciliation with
#     `flinksecret "<id>" not found in namespace`.
#
# Apply order is enforced by sync waves: Secret (10) -> FlinkSecret (20) ->
# FlinkEnvironmentSecretMapping (30) -> catalog (40) -> database (45).
apiVersion: v1
kind: Secret
metadata:
  name: default-cmf-kafka-credentials
  namespace: flink
  annotations:
    argocd.argoproj.io/sync-wave: "10"
type: Opaque
stringData:
  security.protocol: "SASL_PLAINTEXT"
  sasl.mechanism: "OAUTHBEARER"
  sasl.oauthbearer.token.endpoint.url: "http://keycloak.keycloak.svc.cluster.local:8080/realms/confluent/protocol/openid-connect/token"
  sasl.login.callback.handler.class: "org.apache.kafka.common.security.oauthbearer.OAuthBearerLoginCallbackHandler"
  sasl.jaas.config: "org.apache.kafka.common.security.oauthbearer.OAuthBearerLoginModule required clientId='cmf' clientSecret='cmf-secret' scope='profile email';"
---
apiVersion: v1
kind: Secret
metadata:
  name: default-cmf-sr-credentials
  namespace: flink
  annotations:
    argocd.argoproj.io/sync-wave: "10"
type: Opaque
stringData:
  bearer.auth.credentials.source: "OAUTHBEARER"
  bearer.auth.client.id: "cmf"
  bearer.auth.client.secret: "cmf-secret"
  bearer.auth.scope: "profile email"
  # Both endpoint keys are set: the standard Confluent SR client reads
  # `bearer.auth.issuer.endpoint.url`, the FlinkEnvironment's
  # `table.sql-catalog.confluent.schema-registry.*` block uses
  # `bearer.auth.token.endpoint.url`, and which one CMF's bundled SR client
  # honours is undocumented. The unused one is ignored.
  bearer.auth.issuer.endpoint.url: "http://keycloak.keycloak.svc.cluster.local:8080/realms/confluent/protocol/openid-connect/token"
  bearer.auth.token.endpoint.url: "http://keycloak.keycloak.svc.cluster.local:8080/realms/confluent/protocol/openid-connect/token"
---
apiVersion: platform.confluent.io/v1beta1
kind: FlinkSecret
metadata:
  name: kafka-conn-secret-id-default
  namespace: flink
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  secretRef: default-cmf-kafka-credentials
  cmfRestClassRef:
    name: cmf-rest-class
    namespace: flink
---
apiVersion: platform.confluent.io/v1beta1
kind: FlinkSecret
metadata:
  name: sr-conn-secret-id-default
  namespace: flink
  annotations:
    argocd.argoproj.io/sync-wave: "20"
spec:
  secretRef: default-cmf-sr-credentials
  cmfRestClassRef:
    name: cmf-rest-class
    namespace: flink
---
apiVersion: platform.confluent.io/v1beta1
kind: FlinkEnvironmentSecretMapping
metadata:
  name: kafka-conn-secret-id-default
  namespace: flink
  annotations:
    argocd.argoproj.io/sync-wave: "30"
spec:
  flinkEnvironment: default
  secretRef:
    name: kafka-conn-secret-id-default
  cmfRestClassRef:
    name: cmf-rest-class
    namespace: flink
---
apiVersion: platform.confluent.io/v1beta1
kind: FlinkEnvironmentSecretMapping
metadata:
  name: sr-conn-secret-id-default
  namespace: flink
  annotations:
    argocd.argoproj.io/sync-wave: "30"
spec:
  flinkEnvironment: default
  secretRef:
    name: sr-conn-secret-id-default
  cmfRestClassRef:
    name: cmf-rest-class
    namespace: flink

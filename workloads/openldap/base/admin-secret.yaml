---
# Directory admin (cn=admin,dc=confluentdemo,dc=local). Used only by the
# seed Job to load the LDIF below; no CP component binds as this user.
apiVersion: v1
kind: Secret
metadata:
  name: openldap-admin
  namespace: openldap
type: Opaque
stringData:
  LDAP_ADMIN_PASSWORD: admin123

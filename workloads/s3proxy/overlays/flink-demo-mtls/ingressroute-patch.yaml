---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: s3proxy
  namespace: flink
spec:
  routes:
    - match: Host(`s3proxy.flink-demo-mtls.confluentdemo.local`)
      kind: Rule
      services:
        - name: s3proxy
          port: 8000
          scheme: http

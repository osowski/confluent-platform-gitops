# flink-demo cluster-specific overrides for CFK operator
# Minimal overrides - most configuration is in base

# Enable debug logging for homelab troubleshooting
debug: true

# Watch additional namespaces for the colors-and-shapes demo
namespaceList:
  - flink
  - flink-shapes
  - flink-colors
  - kafka
  - operator
